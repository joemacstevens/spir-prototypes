{
  "name": "spir-prototypes",
  "version": "1.0.0",
  "scripts": {
    "build": "cd spir-app && npm install && npm run build && cd .. && rm -rf public && mkdir public && cp index.html public/ && cp -r spir-app/dist public/spir-app && cp -r breathing-module public/breathing-module"
  }
}
